const baseURL = 'http://localhost:8080';

const urls = {
    register: '/register',
    login: '/login',
    players: '/players',
    clubs: '/clubs',
    activate:'/activate'
}

export {
    baseURL,
    urls
}